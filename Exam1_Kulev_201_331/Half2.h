#pragma once
#include <string> // obviously for strings
#include <vector> // :D
#include <set>

int roman_to_arab(std::string roman_number);
std::vector<int> func_Pascal(int k);
int func_substr_len(std::string input_str);
bool setContains(const std::set<char>& d, char element);