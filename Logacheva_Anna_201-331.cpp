#include <iostream>//стандартсная библиотека 
#include <vector>//для вектора
#include "Logacheva_Anna_201-331.h"
#include "Source.cpp"// описани алгоритмов 
#include <fstream>// работа с файлами
using namespace std;
int main()
{
    setlocale(LC_ALL, "rus");
    int num_of_args(vector<bool> f);
    vector<bool> read_from_file(string file_name);
    std::string table(vector<bool> f);

        //int num_of_args(vector<bool> f) done
	//vector<bool> read_from_file (std::string file_name)done
	//bool write_to_file (std::string file_name, vector<bool> f)done
	//std::string table (vector<bool> f)done
	//std::string func_sdnf (vector<bool> f)
	//std::string func_sknf (vector<bool> f)
	//std::string func_Zhegalkin (vector<bool> f)

	// вторая часть экзамена 
	
	string chislo;
	cout << "Введите число на арабском" << endl;
	cin >> chislo;
	int roman_to_arab(string chislo);
	cout << "Введите число" << endl;
	int k;
	cin >> k;
    vector<int>func_Pascal(int k);
	cout << "Введите строку" << endl;
	string stroka;
	cin >> stroka;
	
}
